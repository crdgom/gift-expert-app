import React from 'react';
import ReactDOM from 'react-dom';
import GiftExpertApp from './GiftExpertApp';
import './index.css';
const main = document.querySelector('#root')

ReactDOM.render(<GiftExpertApp />, main);